diff --git a/node_modules/@credo-ts/tenants/build/TenantsApi.mjs b/node_modules/@credo-ts/tenants/build/TenantsApi.mjs
index 6d31ae9..69ffcca 100644
--- a/node_modules/@credo-ts/tenants/build/TenantsApi.mjs
+++ b/node_modules/@credo-ts/tenants/build/TenantsApi.mjs
@@ -85,7 +85,18 @@ let TenantsApi = class TenantsApi {
 		const tenantContext = await this.agentContextProvider.getAgentContextForContextCorrelationId(this.agentContextProvider.getContextCorrelationIdForTenantId(tenantId), { provisionContext });
 		this.logger.trace(`Got tenant context for tenant '${tenantId}'`);
 		const tenantAgent = new TenantAgent(tenantContext);
-		await tenantAgent.initialize();
+		try {
+			await tenantAgent.initialize();
+		} catch (error) {
+			// PATCH(session-release): the session slot was already acquired inside
+			// getAgentContextForContextCorrelationId. If initialize() throws, withTenantAgent's
+			// finally is never reached, so release the slot here or the global session counter
+			// leaks and the pool wedges at the session limit.
+			try {
+				await tenantAgent.endSession();
+			} catch {}
+			throw error;
+		}
 		this.logger.trace(`Initializing tenant agent for tenant '${tenantId}'`);
 		return tenantAgent;
 	}
diff --git a/node_modules/@credo-ts/tenants/build/context/TenantSessionMutex.mjs b/node_modules/@credo-ts/tenants/build/context/TenantSessionMutex.mjs
index 4e20c01..1099b82 100644
--- a/node_modules/@credo-ts/tenants/build/context/TenantSessionMutex.mjs
+++ b/node_modules/@credo-ts/tenants/build/context/TenantSessionMutex.mjs
@@ -40,7 +40,14 @@ var TenantSessionMutex = class {
 		this.currentSessions++;
 		if (this.currentSessions >= this.maxSessions) {
 			this.logger.debug(`Reached max number of sessions ${this.maxSessions}, locking mutex`);
-			await this.sessionMutex.acquire();
+			try {
+				await this.sessionMutex.acquire();
+			} catch (error) {
+				// PATCH(session-release): the post-increment lock timed out. currentSessions was
+				// already incremented above, so undo it before propagating or the slot leaks permanently.
+				this.currentSessions--;
+				throw error;
+			}
 		}
 		this.logger.debug(`Acquired tenant session (${this.currentSessions} / ${this.maxSessions})`);
 	}
